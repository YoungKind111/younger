# 统一管理后台 · 功能与页面清单

| 项 | 内容 |
|---|---|
| 用途 | 统一管理后台的功能范围与页面定义：技术评审（模块边界）、设计评审（出图输入）、与统一管理后台团队对齐（菜单/权限点） |
| 读者 | UI/UE、前端、统一管理后台团队、产品 |
| 依据 | 《统一身份与权限平台产品方案》V1.0（3.1 责任地图、4.2 决策表、4.3 权限体系） |
| 范围 | 统一管理后台两大挂入模块（账号管理、权限模块）+ 平台骨架（组织与员工）+ 全局配置 + 审计 |
| 版本 | V1.0 · 2026-08-28 |

---

## 菜单结构总览

```text
统一管理后台
├── 组织与员工
│   ├── 组织架构（O1）
│   ├── 员工管理（O2 员工列表 / O3 员工详情 / O4 员工开通 / O5 离职处理）
├── 权限管理（RBAC）
│   ├── 角色管理（P1 角色列表 / P2 角色详情）
│   ├── 权限点清单（P3）
│   └── 系统接入（P6，只读清单）
├── 账号管理（认证中心控制台挂入）
│   ├── 账号查询（M1 / M2 账号详情操作）
│   ├── 账号统计（M5）
│   ├── 人工合并（M3）
│   └── 申诉审核（M4）
├── 全局配置
│   ├── 认证源清单（G1，只读）
│   ├── 安全策略（G2）
│   ├── 端与准入清单（G3，只读 + 登记新端）
│   ├── 文案配置（G4）
│   └── 消息模板（G5）
├── 审计中心
│   └── 审计查询（L1）
└── 个人中心（S1 改密与设备）
```

---

## ① 组织与员工（地基模块）

> 员工主数据是账号与权限的前提：先有"在职的人"，才能开账号、授角色；离职触发联动处置（禁账号必执行，企微处置二选一）。

| # | 页面 | 关键交互 | 权限点 | 分期 |
|---|---|---|---|---|
| O1 | 组织架构 | 部门树增删改、调整层级、设置部门负责人；数据范围"本部门"以此树为准 | 组织管理 | 一期 |
| O2 | 员工列表 | 按部门/在职状态/关键字筛选，分页；行内入口：详情、调岗、离职 | 员工查看 | 一期 |
| O3 | 员工详情 | 档案（部门/岗位/在职状态）；调岗（部门/角色 role_ids/数据范围 data_scope）；展示关联 account 状态与最近登录，可跳 M2 | 员工管理 | 一期 |
| O4 | 员工开通 | 填信息建档（**必填**：姓名/手机号/邮箱/部门；**选填**：角色——可先开通、稍后在员工详情授权）→ 邮件邀请链接（72h、一次性）→ 员工激活设密；未激活不可登录（AUTH-120）；**逆向**：链接过期/误删可「重发邀请」（原链接作废、可多次），超 30 天未激活标记「激活逾期」可一键回收 | 员工管理 | 一期 |
| O5 | 离职处理 | 二次确认；①禁账号（≤5s 全端失效，必执行）；企微处置二选一：②禁用企微成员 + ③SCRM 客户继承（默认），或保留企微号继续使用（不触发继承，提示该号仍可直登 SCRM）；逐项展示执行结果与失败重试（AUTH-143） | 员工管理 | 一期 |

## ② 权限管理（RBAC）

> 集中管理、分布执行：角色与授权只在这里维护；各业务系统注册权限点、本地按 token 的 role_ids 校验。
>
> 维护生命周期（详见方案 4.3 权限体系）：新系统由开发**注册**权限点（接口同步/配置导入）；存量系统（如 ERP）走「盘点 → 业务确认 → manifest 注册」；管理员**建角色**（勾选或复制已有角色，P2）→ **授权**（O3）→ 各系统本地**执行** → 变更时增量注册、角色按需补勾。权限点以系统为命名空间，角色可跨系统组合。
>
> 关键机制（吸收 ERP 权限原型实践）：数据范围六档、角色基准档+页面收窄、高风险动作⚠、互斥提醒（不可自审）、草稿/发布 diff。

| # | 页面 | 关键交互 | 权限点 | 分期 |
|---|---|---|---|---|
| P1 | 角色列表 | 角色增删改、复制；启用/停用；展示覆盖系统、数据范围基准档；成员数可点击查看该角色成员名单（按角色查人） | 角色管理 | 一期 |
| P2 | 角色详情（权限配置） | 双视图：默认「已授权」只读表（按系统分组，仅显示已授予项），点「调整权限」进编辑模式——系统 Tab（带已选数）+ 模块折叠（默认展开有授权模块）+ 页面/页签行勾选；**权限树按各系统菜单结构归类**（模块=一级菜单 ▸ 页面=侧边栏入口 ▸ ⧨页签=页内入口，与菜单同源同步）；数据范围=角色基准档（六档），页面级可再收窄、禁止放宽；高风险动作 ⚠；互斥实时提醒；搜索定位；草稿/发布两阶段，发布输出变更 diff。参考实现：ERP 权限配置页 | 角色管理 | 一期 |
| P3 | 权限点清单 | 各业务系统注册的权限点总表，按「系统 ▸ 模块 ▸ 动作」三级分组查看（只读为主；注册由开发随系统上线/改版完成，支持增量注册；已下线权限点标记废弃并提示引用它的角色） | 权限点查看 | 一期 |
| P6 | 系统接入清单（只读） | 已接入系统清单（名称/编码/负责人/接入方式/权限点数/最近同步/状态）；操作：重新同步、废弃权限点处理（提示引用角色）；系统登记与 manifest 发布属接入期开发动作，不在本页 | 策略配置 | 一期 |

## ③ 账号管理（认证中心控制台挂入）

> 管全部 account（不分 C 端/B 端）：C 端账号以查/封/合并/审申诉为主，员工账号多生命周期动作（在 ① 完成）。
>
> 分期说明：M3 人工合并、M4 申诉审核均为二期建设，一期不出原型——一期无存量重复账号；换绑死角（旧号停机/二次放号）一期由客服人工核实兜底，用户侧引导"联系客服"（文案走 G4）。

| # | 页面 | 关键交互 | 权限点 | 分期 |
|---|---|---|---|---|
| M1 | 账号查询 | 列表 + 筛选（关键字/主体类型/状态/注册时间）+ 精确查询；点行进详情：账号状态、login_type、绑定主体列表、最近 10 条登录记录；敏感字段脱敏；查询行为入审计（AUTH-140） | 账号查询 | 一期 |
| M2 | 账号详情（操作） | 禁用/启用（两级：封账号/封主体；原因必填 + 二次验证 AUTH-141/153）；凭证重置（员工：临时密码/重置链接，强制改密 AUTH-142） | 禁用启用 / 凭证重置 | 一期 |
| M3 | 人工合并操作台 | 三步向导：①按手机号/openid 查两个候选账号（绑定主体+业务概况）→ ②合并预览（保留账号、归属规则按合并预案）→ ③二次确认+二次验证执行；结果页含墓碑记录链接（AUTH-144/162） | 人工合并 | **二期**（缓建，见模块说明） |
| M5 | 账号统计 | 账号总量、今日新增/登录；主体类型分布（含未绑定）、账号状态分布、各端登录人次、近 7 日新增趋势（无感知注册占比） | 账号查询 | 一期 |
| M4 | 申诉审核 | 工单列表（待审核/已通过/已驳回）；详情查看材料 → 通过（执行换绑）/驳回（理由必填）；通过/驳回均通知发起人（订阅消息+短信，驳回含理由与重新申诉入口），通知结果入审计（AUTH-133） | 申诉审核 | **二期**（缓建，见模块说明） |

## ④ 全局配置

| # | 页面 | 关键交互 | 权限点 | 分期 |
|---|---|---|---|---|
| G1 | 认证源清单（只读） | 展示各端接入时声明的登录方式能力集与启用状态，供管理员与客服排障查阅；启停与新增登录方式属端开发（二期再评估运营启停开关） | 策略配置 | 一期 |
| G2 | 安全策略 | 失败锁定阈值与时长、会话有效期、密码强度、验证码频率限制；免发版生效，变更留痕（AUTH-145/150） | 策略配置 | 一期 |
| G3 | 端与准入清单（只读） | 端清单（channel）与端×主体准入映射只读展示（客户端小程序/APP=customer、月嫂端小程序/APP=worker、员工端/业务管理后台/SCRM=staff、招聘端=promoter）；「登记新端」为接入期动作（登记时确定准入映射，变更属端开发）（AUTH-102） | 策略配置 | 一期 |
| G4 | 文案配置 | 下发文案管理（禁用提示、登录错误、冷静期提示等）；一期文案写死在各端、上线前法务过审定稿，二期接入本页后免发版 | 策略配置 | **二期** |
| G5 | 消息模板 | 短信/订阅消息/邮件模板管理；一期模板在通道侧配置，二期接入本页统一管理 | 模板配置 | **二期** |

## ⑤ 审计中心

| # | 页面 | 关键交互 | 权限点 | 分期 |
|---|---|---|---|---|
| L1 | 审计查询（统一） | 按操作人/操作对象/类型/时间范围/来源端组合筛选；类型覆盖：认证操作（登录/开户/绑定/合并/注销/禁用/凭证重置/换绑）、权限变更、配置变更；结果只读（AUTH-154） | 审计查询 | 一期 |

## ⑥ 平台自身

| # | 页面 | 关键交互 | 分期 |
|---|---|---|---|
| — | 登录页 | 账号密码 + 企微扫码双方式（走统一认证，memberid 映射 staff）（AUTH-113/116） | 一期 |
| S1 | 个人中心 | 修改密码、查看并踢出自己的登录设备（AUTH-155） | 一期 |

## ⑦ C 端自助页面索引（页面归各业务端承接，本平台提供规范与文案）

> 以下页面为 C 端一期功能（方案 5.1/5.5）的界面载体，**由各业务端产品实现**；本清单只定义页面清单与关键交互规范。文案一期写死在各端（上线前法务过审定稿，清单列为上线检查项），二期接入 G4/G5 配置后免发版。

| 页面 | 归属端 | 关键交互规范 |
|---|---|---|
| 验证码登录页 | 各 C 端 | 手机号 + 6 位验证码；发送频率限制提示；失败计数与锁定提示（文案一期写死，见页首说明） |
| 微信登录页 | 小程序端 | 一键授权；失败后明确提示与重试路径 |
| 用户协议弹窗 | 各 C 端（首登） | 无感知注册后首登强制弹窗；拒绝则退出并清除登录态 |
| 换绑手机号 | 各 C 端 | 新旧号码双验证码；新号已被占用时阻断；旧号不可用/二次放号一期提示"联系客服"（申诉入口二期开放） |
| 绑定/解绑微信 | 各 C 端 | 已登录态下绑定/解绑；解绑后须保留至少一种登录方式否则阻断 |
| 账号注销（三步） | 各 C 端 | 须知 → 前置校验（不通过则告知卡项+处理入口直达）→ 二次确认进冷静期 |
| 冷静期提示页 | 各 C 端 | 期内登录提示剩余天数 + 主动「撤销注销」按钮（登录本身不撤销） |
| 找回密码 | 员工端/后台 | 手机号验证 → 设新密码；旧密码即刻失效、全端 token 吊销 |
| worker 准入引导页 | 月嫂端 | 承接 `guide_status=WORKER_ONBOARDING`：引导至招聘端留资或联系招聘顾问 |

---

## 设计要点

1. **员工（①）与账号（③）分模块**：员工详情管"人"（部门/角色/在职），账号详情管"凭证"（状态/禁用/合并）；两页互跳。对应方案"账号与主体分离"模型——员工是 staff 主体，账号是 account。
2. **客户账号无独立菜单**：客服从 M1 账号查询进，查到谁管谁；不按端分菜单。
3. **权限点粒度**：页面级全注册（控菜单可见），操作级只注册敏感/业务动作（跟随业务确认结果，如跟进/分配/发放）；不要退化成每模块"查看/管理"两档（ERP 实践证明业务动作不可再粗），也不要每个按钮都注册。
4. **P2 参考实现**：ERP 权限配置页（权限RBAC/prototype/role-permission.html）即 P2 的原型基线——角色基准档+页面收窄、高风险⚠、互斥提醒、发布 diff 均已在该原型验证。
5. **统一管理后台自举接入自身 RBAC**：本后台的菜单与操作就是 `admin:` 命名空间下的权限点，在 P2 统一配置（与其他系统同构），不单设菜单配置页。
6. **O5 离职处理为独立页面**：禁账号为必执行，企微处置（禁用并继承 / 保留企微号）二选一，需逐项展示执行结果与失败重试，不宜只是列表里的一个按钮。
7. **状态完整性**：每个表单页需给出空态、加载态、失败态、成功态；账号查询与申诉审核额外给"无结果""已处理"态。
8. **文案管理**：一期提示文案写死在各端（上线前法务过审定稿），二期接入 G4/G5 后配置化、免发版；设计稿用占位符标注文案来源。

## 与统一管理后台团队的对齐点

- 菜单归属与命名：本文菜单结构为建议稿，需与该后台现有菜单体系合并；
- 权限点纳入其 RBAC：本文各页"权限点"列即首批注册的权限点清单；
- 排期依赖：该后台骨架（登录、菜单、RBAC）需早于认证中心第 3 迭代（控制台界面集中在此迭代）。
